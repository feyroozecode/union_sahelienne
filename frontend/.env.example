# API base URL (must include /api/v1 — NestJS globalPrefix + URI versioning)
NEXT_PUBLIC_API_URL=https://api.example.com/api/v1

# Display name in the admin header
NEXT_PUBLIC_APP_NAME=Union Sahelienne Admin
